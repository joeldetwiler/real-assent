# Every path in this repository is owned, so every pull request requires review from the
# owner before it can merge. Keep this list short — a code owner per directory is a thing
# to add when there are people to name, not before.

* @joeldetwiler
